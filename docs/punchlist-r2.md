# Wave 2 punch list (round 2 panel) — authoritative fix list

Owner-resolved forks (honesty-first / brief-faithful — do NOT relitigate):
- Donut: compute TRUE criteria-met % (share of perCriterion.met across the agent's analyses) and keep the 'criteria met' label.
- Timeline latency: relabel to the honest user-perceived boundary 'Response latency · caller stops → agent speaks' (= avgResponseLatencyMs); keep the <1s LiveKit reference; tone honestly (over-target is fine).
- Seed/trend honesty: SHIFT all seed timestamps so the most recent call is ~now (today), making relative times ('4d ago') and any day-over-day delta truthful; drop/relabel the fabricated 'vs. yesterday' so it is real or removed.
- Timeline duration: scale the modeled timeline so its total ≈ the call's real durationSec (no more 46s-vs-258s contradiction); label the axis as modeled.
- Keep 'Use Actions' (brief term) but DEFINE it (and conformance / Modeled timing) inline on first use.

## LOCKED LEXICON (resolve P09 — use these names EVERYWHERE, nothing else)
- **Call score** = `scorecard.overall` (0–100), overall weighted QA score of one call. Replaces "Overall score" AND "Script adherence".
- **Flow adherence** = `flowAlignment.conformanceScore` (0–100), how closely the call followed its expected flow. The ONE name for conformance/drift — retire "Conformance"/"Avg adherence"/"Process fitness" as headline labels (fitness may appear once as a small, clearly-defined secondary stat).
- **Fleet health** = average Call score across the fleet (Overview KPI). Subtitle: "Average call score across agents".
- **Criteria met** = true % of success criteria met (share of `perCriterion.met`), used by the agent donut. NOT avg score.
- Agent "Avg score" = that agent's average Call score.
- Define (tooltip/inline) the FIRST appearance of "Flow adherence", "Use Actions", "Modeled timing".

## Items (31)

### CRITICAL (3)

- **P01 — Agent-detail donut: fix mislabel AND non-informative rendering** _(app/pages/agents/[id].vue (lines 57-59, 296, 309, 313-314) · dataviz)_  
  Two coupled fixes. (1) Mislabel: criteriaMet binds Math.round(health.avgScore) yet the card is titled 'Avg criteria met' with sub-label 'avg met' — it is the overall weighted QA score, not a criteria-met rate, and the SAME number already shows as 'Avg score' in the stat strip two cards away. Retitle to 'Avg call score' / 'avg score' (zero-risk) OR compute a real share-of-perCriterion.met. (2) Rendering: the Unovis VisDonut takes a bare number array [55,45] with a static color array; arcs render only client-side (SSR has none) and the remainder color resolves to --chart-track=--muted (~oklch 0.97) which is invisible white-on-white, so the ring conveys nothing. Feed object data with a value accessor, set --chart-track to a visible neutral track (not --muted), suppress Unovis's default background ring, and verify the teal arc subtends value/360deg. A hand-rolled stroke-dasharray SVG ring is the SSR-stable, always-correct alternative.
- **P02 — Overview Fleet-health delta lies: '-46 vs. yesterday' over stale, non-adjacent seed days** _(app/pages/index.vue (lines 55-61) + seed/trend data · content)_  
  healthDelta = trend.at(-1)-trend.at(-2) is hard-labeled 'vs. yesterday'. Verified: trend = [06-15:90, 06-16:72.5, 06-17:26.7], today is 06-21, so the compared points are 06-16 vs 06-17 (not yesterday) and the data is 4 days stale; the first KPI a reviewer reads asserts an alarming red day-over-day drop that is fabricated. Stop asserting 'yesterday': label by the actual last bucket date ('-46 vs Jun 16' / 'vs 4 days ago'), or drop the delta for sparse synthetic data and let the sparkline carry the trend, reserving a real delta only when >=2 consecutive recent days exist. Best demo fix: regenerate seed so the latest dense day is 'today'.
- **P03 — Shared Table overflows: AgentTable + CallTable hide metric columns (no table-fixed, no cell width caps)** _(app/components/ui/table/Table.vue + AgentTable.vue (first TableCell) + CallTable.vue (contact/agent cells) · layout)_  
  Verified SSR: no table-fixed and no colgroup anywhere. Table.vue emits <table class='w-full'> with auto layout; the AgentTable Agent cell (avatar+name+goal in a flex NuxtLink) and CallTable Contact/Agent cells use truncate but have no bounded width, so the goal/long names balloon the first column and push Status/Avg score/Failures/Use actions off-screen behind a horizontal scrollbar at desktop and worse at tablet/phone. Fix once at the shared layer: add a `fixed?` prop that applies table-layout:fixed, give the text columns a bounded width (colgroup or w-[clamp(...)] on the th) plus max-w-0/min-w-0 on the td so truncate actually clips, and give metric columns fixed widths. This single change repairs the /agents roster, the Overview agents preview, the /calls inbox, and the agent-scoped CallTable together. Pair with a phone-first stacked-cell layout below sm so score/severity are never hidden behind scroll.

### HIGH (10)

- **P04 — Call-header 'Script adherence' is the QA scorecard relabeled (next to a real 'Flow adherence')** _(app/pages/calls/[id].vue (lines 256-265) · clarity)_  
  Verified both labels live in the header. 'Script adherence' is bound to analysis.scorecard.overall — the weighted average of ALL success criteria (outcome/behavior/compliance/tone), not script adherence — and sits one inch from the genuine 'Flow adherence' (conformanceScore), so the operator reads two adherence numbers when one is the composite QA score. Rename the scorecard headline to 'Call score' (or 'QA score') and reserve 'adherence' vocabulary strictly for conformance. This also collapses the Fleet-health/Avg-score/Script-adherence trio into one consistent noun (see P09).
- **P05 — Signature timeline contradicts the call header on duration (46.0s vs 4:18 / 258s)** _(app/components/CallTimeline.vue (line 356, totalSeconds) + server/utils/timeline.ts (totalMs) · dataviz)_  
  Verified for call-support-002: durationSec=258 (header shows 4:18) but timeline.totalMs=46036 so the signature card prints 'Call duration 46.0s' — a 5.6x contradiction inside the element meant to be the credibility showpiece (totalMs sums modeled stage durations with inter-turn silence collapsed). Drop the 'Call duration' KPI from the timeline (the header already states true duration) OR relabel it 'Modeled active time' / 'Pipeline span' and list the caveat in the Modeled-timing popover. Never show two conflicting durations on one screen.
- **P06 — Timeline headline latency label names the wrong boundary (EOU->first audio vs measured user-speech-end->audio)** _(app/components/CallTimeline.vue (line 336) + server/utils/timeline.ts (line ~90) · dataviz)_  
  Verified: avgResponseLatencyMs=1343 is computed as speakStart - lastUserSpeechEnd (starts at END of user speech, includes VAD+EOU+TTFT+TTFB) but is labeled 'Avg response latency (EOU->first audio)'. The named start point (EOU) is one stage later than what's measured, and the metric breaches its own '<1000ms' target partly via a stage the label excludes (EOU p50 is only 589). Either relabel to 'Avg turn turnaround (caller stops -> agent speaks)' or change the math to sum EOU+TTFT+TTFB so it truly is EOU->first audio. Label and computation must use the same boundary the popover already documents.
- **P07 — Breadcrumb renders only 'Overview' server-side on every list/detail page** _(app/layouts/default.vue (lines 73-74, 148-187) + useBreadcrumb.ts + page setBreadcrumb calls · ia)_  
  Verified SSR HTML on /agents and /agents/[id] shows breadcrumb trail = ['Overview'] only — even though /agents/index.vue calls setBreadcrumb([{label:'Agents'}]) synchronously in setup. The useState('breadcrumb') trail isn't reaching SSR output, so the app's only persistent wayfinding conveys nothing until client hydration; on detail pages the watchEffect dependency on async entity data makes it worse. Populate the crumb so it is present in the SSR render (set synchronously from awaited data.value, not in a post-render watchEffect; confirm useState is read after the page write). Separately, reconsider the always-prepended 'Overview' root: Overview is a sibling of Calls/Agents/Settings, not their parent, so the trail asserts a false hierarchy — show the real nav group or just the page name for top-level destinations.
- **P08 — Raw snake_case outcomes render in inbox, call-header badge, and outcome filter** _(app/components/CallTable.vue (line 338) + app/pages/calls/[id].vue (line 240) + app/pages/calls/index.vue (filter options) + useTone (no humanizer) · content)_  
  Verified API outcomes: qualified_not_booked, not_booked, qualified_booked, cancelled, resolved, booked — all printed verbatim. No humanize helper exists anywhere. Snake_case machine tokens on the operator's daily triage queue read as an unfinished dev build. Add one humanizeOutcome() (qualified_not_booked -> 'Qualified, not booked', etc.), route the table cell, header badge, and filter option LABELS through it, keep the raw value as the filter key. Note the header direction badge already has `capitalize` but the adjacent outcome badge does not — visibly inconsistent siblings.
- **P09 — Score vocabulary fragments: ~8 labels for 2 concepts, none defined on first use** _(Global: index.vue (Fleet health), agents/[id].vue (Avg score / Avg criteria met / Avg adherence), calls/[id].vue (Flow adherence / Script adherence), FlowDrift.vue (Conformance / Process fitness) · clarity)_  
  One QA-score quantity appears as Fleet health / Avg score / Avg criteria met / Script adherence; one conformance quantity appears as Flow adherence / Avg adherence / Conformance / Process fitness — and a newcomer cannot tell which are the same. Adopt a two-term system and use the EXACT strings everywhere: 'Call score' (or 'Score') for scorecard.overall, 'Flow adherence' for conformanceScore. Demote 'Process fitness' to a tooltip detail under Flow adherence (or relabel 'Required steps completed') rather than a second 30px headline. Add a one-line first-use tooltip teaching the two-axis model (did it follow the steps x did it meet the criteria). Several sub-items (P04 rename, P01 retitle) are part of executing this system.
- **P10 — Recurrence count computed but discarded — cross-agent 'this fix recurs across N calls/M agents' is invisible** _(server/utils/rollup.ts (topRecommendations, lines 134-185) + RecommendationItemSchema (shared/types.ts) + RecommendationCard.vue · brief-fidelity)_  
  Verified: the rollup dedupes the same advice across calls and increments `count` (line 149) and sorts by it, but RecommendationItemSchema has no count/callCount/agentCount field, so the value is thrown away. Every card shows a single source call+agent, making a systemic 3-agent script bug look identical to a one-off — the brief's strongest 'across agents' insight, already in the data, is never shown. Add callCount (and ideally agentCount/agent list) to RecommendationItem, populate from the bucket, and render a chip on RecommendationCard ('Seen on 4 calls across 3 agents'). Cheapest, highest-leverage way to make 'visualize issues across agents' actually visible.
- **P11 — Cross-agent roster can't compare on the signature metric (no Flow adherence column)** _(app/components/AgentTable.vue + Overview agents preview + AgentHealth payload · brief-fidelity)_  
  Verified: AgentTable has zero conformance/adherence references; its columns are Status/Avg score/Failures/Use actions. The only cross-agent comparison surface therefore cannot rank agents on flow adherence — the expanded vision's designated differentiator — even though flowSummary.avgConformance is already computed per agent. Thread avgConformance into AgentHealth/the roster payload and add a sortable 'Flow adherence' column toned via useTone, giving the brief's 'visualize issues across agents' requirement a real fleet-level home on the product's own axis.
- **P12 — Flow conformance (differentiator #2) buried behind the 4th, non-default tab** _(app/pages/calls/[id].vue (Tabs default-value='findings') + FlowDrift.vue · expanded-vision)_  
  Verified: the default call-detail view contains NO 'Conformance' or 'Process fitness' string — the entire conformance experience (score, band label On track/Some drift, tinted flow graph, drift list) lives only inside the 'Flow drift' tab, reachable only if the user clicks jargon. The signature expected-vs-actual analysis is invisible by default. Promote the conformance summary (score + band + tinted graph) to a persistent card directly under the scorecard at the same altitude as Findings; the drift LIST can stay in a tab. At minimum surface a flow-drift summary chip in the Findings tab that deep-links into the Flow tab.
- **P13 — Transcript<->timeline<->flow cross-highlight only half-wired (no persisted selection)** _(app/pages/calls/[id].vue (TranscriptViewer/CallTimeline/FlowDrift bindings, lines 301-306, 530-533, 465-471) · interaction)_  
  The supporting signature (spec W19) is incomplete: the page never holds a shared activeTurnIdx/activeNodeId and never passes :active-idx to TranscriptViewer, :active-turn-idx to CallTimeline, :active-node-id to FlowDrift, nor binds @select-turn from the transcript. TranscriptViewer DOES render turns as <button> with aria-pressed/focus-visible by default (selectable!==false), and CallTimeline already implements the s.active ring + opacity-40 dimming and emits selectTurn — but with no activeTurnIdx passed, clicking a timeline bar produces only a transient 1.6s transcript flash and no persisted state on any surface, and transcript->elsewhere is dead. Hold activeTurnIdx + activeNodeId on the page, pass them to all three components, and bind @select-turn so a clicked/keyboarded turn persists the accent ring everywhere. Keep the flash as an additional scroll cue.

### MEDIUM (13)

- **P14 — 'Use Action segment' label repeats on every turn in a band (7x for one action), diluting the signal** _(app/components/TranscriptViewer.vue (lines 139-144) · interaction)_  
  Verified: call-support-002 renders 'Use Action segment' 7 times (one coach action spanning turns 1-7). The label is gated purely on bandMap membership, so longer ranges get louder, not clearer, turning a meaningful annotation into wallpaper and undercutting the brief's 'specific call segments needing human intervention'. Render the label only on the band's first turn (keep the left-band on every turn for continuity), and show the action's kind/reason once at the band head ('Use action - Coach agent') linking to the Use Actions tab. Consider capping/prioritizing Use Actions per call so the KPI reads as a real worklist length.
- **P15 — Recommendation titles mix Title Case and sentence case in the same grid** _(server/services/llm (analysis prompt) + RecommendationCard.vue (line ~103) + FindingCard.vue · content)_  
  Verified live API: 'Update Script to Correctly State Incentives' and 'Train Agents on Identifying and Acting Upon Buying Signals' (Title Case) sit beside 'Correct pricing information' and 'Clarify credit application timeline.' (sentence case), with inconsistent terminal punctuation and length. On the payoff surface (the fix-queue grid) this reads as raw model output, violating the spec's locked sentence-case rule. Two-layer fix: (1) instruct the analysis prompt to emit sentence-case, imperative, <=8-word titles with no trailing period; (2) defensively normalize on render with toSentenceCase() preserving acronyms/proper nouns, so consistency holds even when the model drifts.
- **P16 — Three signature concepts (Use Actions, flow conformance, Modeled timing) never defined for a first-time user** _(app/pages/calls/[id].vue (Use Actions tab, KPI), AgentTable column, FlowDrift header, CallTimeline lanes · clarity)_  
  'Use Actions' is the assignment's coined term and the brief explicitly asks the product to highlight it, yet no screen defines what a use-action IS (a call segment needing human review/coaching/script training). Conformance/drift jumps straight to a number with no process-mining gloss, and the timeline lanes assume VAD/STT/EOU/TTS literacy. Add microcopy (not new widgets): a tooltip/info-icon on the Use Actions tab and 'Open use actions' KPI, a one-line caption under the first conformance number, and keep raw pipeline acronyms inside lane tooltips only. Highest clarity-per-effort.
- **P17 — Settings hides the scoring model behind 'Configured by your workspace'** _(app/pages/settings/index.vue (Analysis model card, lines ~249-273) · content)_  
  Verified rendered: the card shows only 'Analysis model' + 'Configured by your workspace' with no provider/model, despite the code knowing it (anthropic/ollama/mock; New agent already shows 'generated by {provider}/{model}'). For an embedded marketplace app the table-stakes 'which model scores my calls' is answered with a placeholder, inconsistent with New agent. Expose the resolved provider+model read-only (e.g. 'Anthropic / claude-...' or 'Local / qwen...') via /api/context, keeping keys server-side. If it must stay hidden, at least say 'Managed model - set by your admin' rather than the contentless phrase.
- **P18 — actualPath (the traversed call path) computed but rendered nowhere — expected-vs-actual is never explicit** _(server/utils/conformance.ts (returns actualPath) + FlowDrift.vue / FlowDiagram.vue · expanded-vision)_  
  Verified: actualPath has zero consumers in app/. The one datum that makes 'process mining: actual vs design intent' visually obvious — the ordered list of nodes the call truly traversed — ships in the payload but is never shown; the user only sees the expected graph tinted by per-node status and must infer the actual sequence. Render it explicitly: a compact 'Actual path' lane beside the expected spine, or an ordered breadcrumb strip ('Greeting (skipped) -> Acknowledge -> ... -> Close') above the drift list, calling out skipped/extra nodes inline. Converts buried dots into a one-glance designed-vs-happened story.
- **P19 — Agent-detail Expected-flow graph not fused with the agent's drift data (static, no skip-rate overlay)** _(app/pages/agents/[id].vue (lines 253-263, FlowDiagram :interactive=false, no :alignment) · expanded-vision)_  
  The agent page has both the expected flow and the aggregate drift data ('Offer Retention 100% skipped', 'Greeting 50%') but renders them disconnected: the FlowDiagram gets no alignment prop, so it's a static design diagram while drift lives only in a separate bar list above it. The per-agent view is exactly where 'this node is skipped 100% of the time' should be painted onto the node. Feed an aggregate alignment / per-node skip-rate heat value into the agent-detail FlowDiagram so each node is tinted by its fleet drift rate, turning the static diagram into the agent's drift heatmap.
- **P20 — Scorecard per-criterion bars are all teal regardless of pass/fail (color contradicts the dot/number)** _(app/pages/calls/[id].vue (lines 335-338, <Progress :model-value=cs.score>) · dataviz)_  
  The per-criterion Progress indicator renders --primary teal for every criterion while the adjacent dot and number ARE status-toned, so a failed criterion (red dot, red number, score 0-20) shows a teal/success-colored bar — the bar encodes no status and visually softens a red-flag result. Tint the indicator by scoreToneName(cs.score) the way FlowDrift's conformance bar already does ([&_[data-slot=progress-indicator]]:bg-success/-warning/-danger) with a matching -soft track, so the bar reinforces the dot+number.
- **P21 — Calls inbox not phone/tablet usable; triage signals hidden behind horizontal scroll** _(app/components/CallTable.vue responsive column strategy + app/pages/calls/index.vue · responsive)_  
  Below the table-fixed fix (P03), the inbox still needs a phone-first reflow: at 390px only Contact+Agent are reachable and Score/Top severity/Outcome scroll off; the sm/md column show/hide plan re-shows all columns at md and overflows ~768px too. Collapse each call below sm to a 2-line stacked cell (line 1 Contact + right-aligned toned Score; line 2 Agent . relative time . SeverityBadge) so score/severity are always visible; reveal Direction/Findings only at lg where they fit. Never h-scroll the table inside a 356-718px pane.
- **P22 — Warning-tone badges fail WCAG contrast (~1.8:1 light text on near-white soft)** _(app/composables/useTone.ts (warning badge = bg-warning-soft text-warning) — affects 'Awaiting review', 'Modeled timing', drift labels, use-action chip · a11y)_  
  useTone's warning badge pairs text-warning (oklch ~0.75) on bg-warning-soft (oklch ~0.96) for roughly 1.8:1 contrast, failing the 4.5:1 minimum across several visible chips. Switch warning badges to bg-warning-soft with text-warning-foreground (the darker foreground token), and audit success-soft / danger-soft badges to confirm 4.5:1.
- **P23 — Settings connection chip stalls on an unlabeled 3s 'Checking' spinner inside the iframe** _(app/pages/settings/index.vue + app/composables/useGhlBridge.ts (fixed 3000ms timeout) · interaction)_  
  In the production-like iframe the connection chip shows an unexplained 'Checking' spinner with pulsing skeletons for a hard-coded 3s before resolving to 'Not connected', because the parent never replies to REQUEST_USER_DATA and only the timeout ends the wait — with no progress copy or retry. Resolve optimistically (if a URL locationId exists, set ready immediately), shorten the handshake (~800ms) with a labeled state ('Resolving your HighLevel session...'), and add a 'Retry connection' affordance. Never leave an unlabeled spinner as the only state for 3s.
- **P24 — Data-table rows advertise full-row click-through but only the first cell is a link** _(app/components/CallTable.vue + AgentTable.vue (TableRow has cursor-pointer; only cell[0] wraps NuxtLink) · interaction)_  
  Rows carry `group cursor-pointer` (spec mandates full-row click-through + ChevronRight) but only the first cell is a NuxtLink; Status/Score/Failures/Use-actions/Outcome cells are inert to mouse and keyboard, so the pointer cursor over dead cells misleads and the click target is a fraction of the row. Use a stretched-link pattern (one absolutely-positioned NuxtLink overlay per row) or route a row @click while keeping the first-cell link for keyboard/SR — one tab stop per row, with the ChevronRight inside the activation target. Or drop cursor-pointer from non-interactive areas.
- **P25 — Overview two-column work area is a dead L-shape; trend strip too tall** _(app/pages/index.vue (lg:grid-cols-5 agents col-span-3 vs recs col-span-2) + Fleet health trend SectionCard · layout)_  
  The wide agents column (~287px) is far shorter than the narrow recommendations column (~968px) because each RecommendationCard is tall, leaving ~680px of dead space; the bounded 5-agents/4-recs previews don't equalize. Either swap ratios (give recommendations the wide col-span-3, agents the narrow) or height-match the recs preview to the agents table with a 'See all N' link. Separately the 'Fleet health trend' card renders ~292px though HealthChart gets height=180 — tighten to ~180-200px (inline the caption, dense body padding, chart height ~140) to reclaim above-the-fold space.
- **P26 — Call-detail transcript card stretches past its fixed-height scroll viewport (large blank gap)** _(app/pages/calls/[id].vue (lg:grid-cols-2) + TranscriptViewer.vue (ScrollArea h-[calc(100svh-13rem)]) · layout)_  
  The grid stretches both columns to the taller scorecard side (~1379px) but TranscriptViewer's ScrollArea is hard-capped at h-[calc(100svh-13rem)] (~692px), leaving a ~687px empty gap inside the bordered transcript card; the magic 13rem offset also breaks when the header wraps at 768px. Drop the inner ScrollArea and share page scroll, OR make a deliberate two-pane split with both columns lg:sticky/max-h to a measured header-height variable rather than a hardcoded 13rem. The transcript card must not stretch past its scrollable content.

### LOW (5)

- **P27 — Inconsistent KPI sparkline anatomy — only Fleet health carries a spark (reads as an orphaned stub)** _(app/pages/index.vue (line 185, :spark only on Fleet health) + KpiCard.vue (VisArea, no VisLine) · visual)_  
  Verified only the Fleet-health KPI passes :spark; with 3 trend points the lone low-opacity VisArea degenerates into a faint vertical stub at the card's right edge (the round-1 'glitch'), and the inconsistent treatment is exactly what W13 set out to remove. Apply W13: spark on all trend-bearing KPIs or none. Simplest: drop the lone spark (the dedicated trend strip below already shows the series) so all four KPIs share one clean anatomy; if kept, add a VisLine stroke and a minimum width so it reads as a trend.
- **P28 — 'Process fitness' caption overstates what the number measures (excludes out-of-order hits)** _(app/components/FlowDrift.vue (line 131-136) + server/utils/conformance.ts (requiredHit filter) · dataviz)_  
  'Process fitness' is captioned 'Share of the designed flow the call actually replayed', but fitness counts a required node as replayed only if hit AND in order (conformance.ts excludes isOutOfOrder), so a step that demonstrably occurred (just out of sequence) is reported as not replayed — the caption contradicts the math, collapsing coverage and ordering into one number. Either reword to 'Share of required steps hit in the designed order' or compute fitness as pure coverage and let conformanceScore carry the ordering penalty (it already does).
- **P29 — Polish pass: document-title separators, sync-button verb, timeline title case, agent-goal/node-label casing** _(useHead titles across pages; settings vs calls 'Sync calls' button; CallTimeline title; agents/[id] header goal + flow node labels · content)_  
  Bundle of locked-spec polish: (1) document titles use three separator conventions (middot / em-dash / none) and one is reversed ('{contact} . Call') — add one titleTemplate in app.vue ('{Page} . Voice AI Copilot'). (2) The sync action is 'Sync calls from HighLevel' on the inbox but 'Sync calls' on Settings (whose card title already says 'from HighLevel') — pick one verb. (3) Card title 'Voice Pipeline Timeline' is Title Case against the locked sentence-case scale (the component's own aria-label uses 'Voice pipeline timeline'); fix it (keep the intentional 'Use Actions'). (4) Agent-detail header goal isn't clamped (wraps 2 lines) and flow node labels render Title Case ('Offer Retention Option') — add line-clamp and sentence-case the node labels.
- **P30 — Timeline color encodes lane identity (static) and surfaces STT as a cost chip, not data-driven latency** _(app/components/CallTimeline.vue (LANES hard-coded tones; stageChips includes STT) · dataviz)_  
  The cost-lane coloring is static-by-stage (EOU always amber, LLM always red) rather than driven by each segment's latencyMs vs its modeled budget, so color reverts to decoration-by-category instead of 'where THIS call spent time'; and the headline 'Per-stage p50' chips lead with STT (280ms) though STT is declared a non-cost lane, muddying the 'where time is spent' story. Drive cost-lane saturation from latencyMs vs budget (within budget = muted, over = warning/danger) and drop STT from the headline chips so the four chips are exactly the cost stages (EOU / LLM TTFT / TTS TTFB / e2e).
- **P31 — Filterable surfaces handle deep-linked agent scope inconsistently (calls inbox lacks a removable scope chip)** _(app/pages/calls/index.vue vs app/pages/recommendations/index.vue · ia)_  
  Recommendations shows a 'Filtered to' chip with a clear-X for a deep-linked agent scope; the calls inbox with ?agentId= surfaces scope only via the Select and result text, with no removable chip or link back to the agent. Give the calls inbox the same removable scope chip (agent name linking to /agents/[id]); unify into a shared FilterScopeChip. Also add prev/next call navigation on /calls/[id] (spec W31, unaddressed) so triage doesn't force a round-trip to the inbox between every call.
